[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "photo-album"
version = "0.1.0rc1"
description = "A multilingual desktop application for creating chronological photo albums"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "GPL-3.0-or-later" }
authors = [
    { name = "Photo Album Contributors" }
]
dependencies = [
    "Pillow>=10.0",
    "pypdf>=5.0",
    "reportlab>=4.0",
    "PySide6>=6.6"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0"
]

packaging = [
    "build>=1.2",
    "pyinstaller>=6.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.setuptools.packages.find]
where = ["src"]


[tool.setuptools.package-data]
"photoalbum.resources" = [
    "icons/*.svg",
]
"*" = [
    "manifest.json",
    "README.md",
    "screenshots/*",
    "assets/*",
    "assets/**/*",
    "docs/*",
]

[project.scripts]
photo-album = "photoalbum.gui.application:main"
photo-album-cli = "photoalbum.cli:main"
pa = "photoalbum.gui.application:main"
